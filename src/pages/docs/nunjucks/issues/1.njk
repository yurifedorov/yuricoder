{% extends "base.njk" %}

{% set file_name   = 'issue-1' %}
{% set category    = 'docs' %}
{% set subcategory = 'nunjucks' %}
{% set segment     = 'issues' %}
{% set subsegment  = 'issue-1' %}

{% block head %}
  <title>Не возможно задать имя массива через переменную</title>
  <meta name="description" content="Неприятные ситуации в процессе работы с шаблонизатором Nunjucks" />
  <link rel="stylesheet" href="{{prism_css}}">
  <script src="{{prism_js}}" async></script>
{% endblock %}

{% block main %}

  <h1>задать имя массива через переменную</h1>
  <p>Если сделать так, как я описал - то будет ошибка "Template render error"</p>

  <section>
    <h2>Шаблон блока tile</h2>
    <p>хочу задать массив docs через переменную - вот так { { array } } и чтобы на это место подставилось нужное значение.</p>
    {# example1 #}
    {% set example_id = 'example1' %}
    {% set numeration = false %}
    {% set split      = false %}
    {% set html       = false %}
    {% set njk        = true %}
    {% set css        = false %}
    {% set js         = false %}
    {% set demo       = false %}
    {% include codeExample %}
  </section>

  <section>
    <h2>Страница, где будет блок tile</h2>
    <p>генерация будет на основе data.json, поэтому хотелось бы просто указать имя массива и ниже вставить блок (всего 2 строки).</p>
    {# example2 #}
    {% set example_id = 'example2' %}
    {% set numeration = false %}
    {% set split      = false %}
    {% set html       = false %}
    {% set njk        = true %}
    {% set css        = false %}
    {% set js         = false %}
    {% set demo       = false %}
    {% include codeExample %}
  </section>

  <section>
    <h2>Собственно data.json</h2>
    <p>Пример массива.</p>
    {# example2 #}
    {% set example_id = 'example3' %}
    {% set numeration = false %}
    {% set split      = false %}
    {% set html       = false %}
    {% set njk        = true %}
    {% set css        = false %}
    {% set js         = false %}
    {% set demo       = false %}
    {% include codeExample %}
  </section>

{% endblock %}